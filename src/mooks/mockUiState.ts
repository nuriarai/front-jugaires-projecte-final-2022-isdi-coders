import { UiState } from "../types/uiTypes";

const mockUiState: UiState = {
  isLoading: false,
  modal: {
    isOpen: false,
    message: "",
    modalType: "none",
  },
};

export default mockUiState;
